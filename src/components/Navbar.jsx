import logo from "../assets/logo.png"

const Navbar = () => {
  return (
    <nav>
            <img src={logo} alt="" />
            <span>Student Dashboard</span>
        
    </nav>
  )
}

export default Navbar